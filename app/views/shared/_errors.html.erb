<% if @user.errors.any? %>
  <div id="errors">
    <h2><%= pluralize(@user.errors.full_messages.count, "error") %></h2>
    <ul>
    <% @user.errors.each do |field, msg| %>
      <li><%= field %>: <%= msg %></li><br>
    <% end %>
    </ul>
  </div>
<% end %>